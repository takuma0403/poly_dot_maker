# ---- Development Stage ----
FROM golang:1.25-alpine

RUN go install github.com/air-verse/air@latest

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

# Source (src/) and static files (static/) are provided via volume mount at runtime
CMD ["air", "-c", ".air.toml"]
